import React from "react"
import ReactLoading from 'react-loading';

const MyLoader = (props) => (
 <div>
    <ReactLoading type={'spokes'} color={'orange'} height={667} width={375} />
    </div>
)

export default MyLoader

